from .game import player, LEFT, RIGHT, UP, DOWN

direction = DOWN
player.walk(direction)
player.walk(direction)
player.walk(direction)
player.walk(direction)

direction = UP
player.walk(direction)
player.walk(direction)

direction = RIGHT
player.walk(direction)
player.walk(direction)

direction = DOWN
player.walk(direction)
player.walk(direction)

direction = LEFT
player.walk(direction)
player.walk(direction)
