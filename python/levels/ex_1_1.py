from .game import player, LEFT, RIGHT, UP, DOWN

iterator = 1

while (iterator <= 4): # While the condition is true, the code will be repeated
    # Write your code here
    iterator = iterator + 1
    # At the end of the loop here, the variable 'iterator' will be increased by 1

# This is out of the while